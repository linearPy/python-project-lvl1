name: linter-check

on: push

jobs:
  build:
    runs-on: ubuntu-latest
	steps:
	  - name: Install dependencies
	    run: |
		  pip install poetry
		  make install
	  - name: Run linter
	  	  make lint
